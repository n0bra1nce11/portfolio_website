---
import BaseLayout from '@/layouts/BaseLayout.astro';
---

<BaseLayout
  title="Experience"
  description="Professional scope and capability: web and network VAPT, IT audit against ISO 27001, and SIEM development."
>
  <article class="container prose">
    <h1>Experience</h1>

    <p>
      Client engagement details — organization names, specific findings, dates, and scope — are
      confidential under NDA and aren't listed here. What follows is the shape of the work: what
      I've done, not who I've done it for. The <a href="/work/hospital-vapt/">hospital
      management system VAPT</a> in Work is offered as a sanitized work sample built on my own
      lab infrastructure, in the same format and depth as client deliverables.
    </p>

    <div class="callout">
      TODO — replace each block below with real role titles, employers/clients (where
      disclosable), and dates once available. Keep the "scope, not specifics" framing for
      anything still under NDA.
    </div>

    <section class="role">
      <div class="role-head">
        <h2>Web &amp; network VAPT</h2>
        <p class="mono role-dates">TODO — dates</p>
      </div>
      <p class="mono role-org">TODO — role title / organization (or "confidential — NDA")</p>
      <ul>
        <li>Web application testing against the OWASP WSTG methodology.</li>
        <li>Network-layer testing following PTES phases (recon through reporting).</li>
        <li>TODO — scope of systems tested (web, API, internal network, cloud) without naming
          the client.</li>
      </ul>
    </section>

    <section class="role">
      <div class="role-head">
        <h2>IT audit — ISO/IEC 27001</h2>
        <p class="mono role-dates">TODO — dates</p>
      </div>
      <p class="mono role-org">TODO — role title / organization (or "confidential — NDA")</p>
      <ul>
        <li>Gap assessment against ISO/IEC 27001:2022 Annex A controls.</li>
        <li>TODO — Statement of Applicability work, evidence collection, or audit support
          scope, described generically.</li>
      </ul>
    </section>

    <section class="role">
      <div class="role-head">
        <h2>SIEM development</h2>
        <p class="mono role-dates">TODO — dates</p>
      </div>
      <p class="mono role-org">TODO — role title / organization (or "confidential — NDA")</p>
      <ul>
        <li>Wazuh deployment, rule tuning, and alert triage workflow design — see the
          <a href="/work/wazuh-siem-build/">Wazuh SIEM build</a> for the lab version of this
          work.</li>
      </ul>
    </section>
  </article>
</BaseLayout>

<style>
  .callout {
    border: 1px solid var(--rule-strong);
    border-left: 3px solid var(--accent);
    padding: var(--space-2) var(--space-3);
    margin-block: var(--space-3);
    font-size: var(--fs-sm);
  }

  .role {
    border-top: 1px solid var(--rule);
    padding-block: var(--space-3);
  }

  .role-head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: var(--space-2);
    flex-wrap: wrap;
  }

  .role-head h2 {
    margin: 0;
    font-size: var(--fs-lg);
  }

  .role-dates,
  .role-org {
    color: var(--fg-muted);
    font-size: var(--fs-sm);
  }

  .role-org {
    margin-top: 0.1rem;
  }
</style>
